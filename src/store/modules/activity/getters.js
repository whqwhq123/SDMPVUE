/**
 * @author yuhongru
 * @data 2019/5/10 10:58
 **/
const getters = {
  list: (state) => state.list,
  imageInfo: (state) => state.imageInfo,
  listOfPriceActivity: (state) => state.listOfPriceActivity,
  selectedActivityList: (state) => state.selectedActivityList
};
export default getters;
