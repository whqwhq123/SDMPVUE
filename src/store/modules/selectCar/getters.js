/**
 * @author yuhongru
 * @data 2019/5/10 10:58
 **/
const getters = {
  modelName: (state) => state.modelName,
  styleFullName: (state) => state.styleFullName,
  carModel: (state) => state.carModel,
  car: (state) => state.car
};
export default getters;
